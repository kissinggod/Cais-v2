extends base4
+load('/template/css/blog_basic.css /template/style/css/style.scss /template/css/fonts.css')
load("/template/js/jquery2-latest.js")
load("/template/js/scroll.js")

#top

body

block content
	div#posts
	    ul.list-with-title
	        entries = get_data(type='post', status=['public', 'secret'], limit=20).group('-date:year')
	        for year, posts in entries
	            div.listing-title= year
	            ul.listing
	                for post in posts
	                    .listing-item
	                        .listing-post
	                             a(href=post.url, title=post.title)= post.title
	                             div.post-time
	                                span.date= post.date.format("%m-%d")


	.pager
		if pager.has_pre
			a.pre(href=pager.pre_url) Pre
		if pager.has_next
			a.next(href=pager.next_url) Next